{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the blind_browser desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
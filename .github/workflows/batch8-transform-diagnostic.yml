name: Batch 8 Transform Diagnostic

on:
  push:
    branches:
      - agent/batch8-privacy-controls
    paths:
      - .github/batch8-diagnostic-trigger.txt

permissions:
  contents: read
  issues: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Run transformation and privacy audit diagnostic
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set +e
          python3 scripts/batch8-v2-apply.py >"${RUNNER_TEMP}/batch8-diagnostic.log" 2>&1
          status=$?
          if test "${status}" -eq 0; then
            python3 scripts/check-sensitive-diagnostics.py >>"${RUNNER_TEMP}/batch8-diagnostic.log" 2>&1
            status=$?
          fi
          {
            printf 'Batch 8 privacy-audit diagnostic. Head: %s. Run: %s. Exit: %s.\n\n```text\n' "${GITHUB_SHA}" "${GITHUB_RUN_ID}" "${status}"
            tail -c 50000 "${RUNNER_TEMP}/batch8-diagnostic.log"
            printf '\n```\n'
          } >"${RUNNER_TEMP}/batch8-comment.md"
          gh issue comment 5 --repo "${GITHUB_REPOSITORY}" --body-file "${RUNNER_TEMP}/batch8-comment.md"
          exit "${status}"
